# Generated profiles

Use `python -m simulator profiles build ...` to create the normalized
msModeling analytic profile JSON consumed by the simulator. Generated JSON and
raw traces are intentionally ignored because a full 128K grid can be large and
is specific to an msModeling revision and device profile.
