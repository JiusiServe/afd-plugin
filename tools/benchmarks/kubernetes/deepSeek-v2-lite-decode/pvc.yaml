apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: deepseek-v2-lite-pvc
  labels:
    app: afd-recipe
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ibm-spectrum-scale-fileset
  resources:
    requests:
      storage: 100Gi
